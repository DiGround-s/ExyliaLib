package net.exylia.lib.api.aimtrainer;

/**
 * Where a solo session is: counting down, running, or resting in the arena
 * after the drill with the summary on screen, from where a punch replays it.
 *
 * @since 1.5.0
 */
public enum AimSessionPhase {
    COUNTDOWN,
    RUNNING,
    RESTING
}
